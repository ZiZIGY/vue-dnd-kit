import{_ as s,o as i,c as a,ak as t}from"./chunks/framework.DQ-h6x7C.js";const c=JSON.parse('{"title":"Changelog","description":"","frontmatter":{},"headers":[],"relativePath":"v2/guide/changelog.md","filePath":"v2/guide/changelog.md"}'),n={name:"v2/guide/changelog.md"};function o(r,e,l,d,h,p){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="changelog" tabindex="-1">Changelog <a class="header-anchor" href="#changelog" aria-label="Permalink to &quot;Changelog&quot;">​</a></h1><h2 id="v2-3-0-2026-03-26" tabindex="-1">v2.3.0 <span style="font-size:0.6em;font-weight:400;color:var(--vp-c-text-3);margin-left:0.5rem;">2026-03-26</span> <a class="header-anchor" href="#v2-3-0-2026-03-26" aria-label="Permalink to &quot;v2.3.0 &lt;span style=&quot;font-size:0.6em;font-weight:400;color:var(--vp-c-text-3);margin-left:0.5rem&quot;&gt;2026-03-26&lt;/span&gt;&quot;">​</a></h2><h3 id="new-feature-—-stable-id-for-virtual-lists" tabindex="-1">New feature — stable <code>id</code> for virtual lists <a class="header-anchor" href="#new-feature-—-stable-id-for-virtual-lists" aria-label="Permalink to &quot;New feature — stable \`id\` for virtual lists&quot;">​</a></h3><p><code>makeDraggable</code> now accepts an optional <code>id</code> option:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">makeDraggable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(el, { id: item.id }, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [index, items]);</span></span></code></pre></div><p><strong>Why it exists.</strong> In a virtualized list (e.g. <code>@vueuse/core</code> <code>useVirtualList</code>) items are unmounted when they scroll out of view and remounted when they scroll back. Without a stable id, the library had no way to know that the freshly mounted <code>&lt;div&gt;</code> is the same item that is currently being dragged — so it appeared as a second, ghost-like hover target. Drop also failed after a scroll-away/scroll-back cycle because the internal reference to the initiating element went stale.</p><p><strong>How it works.</strong> The id is written to a <code>data-dnd-kit-draggable</code> attribute on the element. Even after an unmount/remount cycle, the library reads this attribute, scans <code>draggingMap</code> for a matching id, and seamlessly remaps the entry to the new DOM node. No user action required — just pass <code>id</code>.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Survives unmount → remount while dragging</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">isDragging</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> makeDraggable</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(el, { id: props.item.id }, () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  props.index,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  props.items,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">]);</span></span></code></pre></div><p><code>id</code> is optional. If omitted a random id is generated (same behavior as before, virtual lists just didn&#39;t work reliably).</p><p>→ <a href="/vue-dnd-kit/v2/examples/performance.html">Performance example</a> — live demo with 200 virtualised items → <a href="/vue-dnd-kit/v2/guide/core/make-draggable.html#options">makeDraggable — option <code>id</code></a></p><hr><h3 id="new-export-—-injectionkey" tabindex="-1">New export — <code>injectionKey</code> <a class="header-anchor" href="#new-export-—-injectionkey" aria-label="Permalink to &quot;New export — \`injectionKey\`&quot;">​</a></h3><p><code>injectionKey</code> is now part of the public API:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { injectionKey } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@vue-dnd-kit/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>This is the Vue injection key under which <code>DnDProvider</code> stores its internal context. It is intended for <strong>advanced or non-standard use cases</strong> — for example:</p><ul><li>Bridging a separately mounted Vue app (e.g. a shadow root) into the same DnD context</li><li>Building custom integrations that need direct access to the internal provider</li><li>Exploring or debugging what the library holds internally</li></ul><p>For everything standard — reading drag state, pointer position, preview, entities — use <code>useDnDProvider</code>. It returns a stable, typed public API and is all you need in 99% of cases.</p><p><strong><code>injectionKey</code> gives you the raw internal provider.</strong> Its shape is not part of the public API contract and may change between minor versions.</p><p>→ <a href="/vue-dnd-kit/v2/guide/core/use-dnd-provider.html#injectionkey-advanced-use-only">injectionKey — advanced use</a></p><hr><h3 id="bug-fix-—-drop-fails-after-scrolling-away-while-dragging" tabindex="-1">Bug fix — drop fails after scrolling away while dragging <a class="header-anchor" href="#bug-fix-—-drop-fails-after-scrolling-away-while-dragging" aria-label="Permalink to &quot;Bug fix — drop fails after scrolling away while dragging&quot;">​</a></h3><p><strong>Symptom.</strong> Start dragging an item in a virtual list → scroll so the item unmounts → scroll back so it remounts → try to drop — nothing fires.</p><p><strong>Root cause.</strong> <code>buildDraggedItems</code> (internal) used <code>draggingMap.get(initiatingElement)</code> to look up the active drag. After a remount <code>initiatingElement</code> pointed to the old, detached DOM node that was no longer a key in <code>draggingMap</code>.</p><p><strong>Fix.</strong> All internal drag-entity lookups now go through <code>resolveFromDraggingMap</code>, which first tries the direct map lookup (fast path) and falls back to scanning by the <code>data-dnd-kit-draggable</code> attribute id. Because the id is stable across remounts, the correct entity is always found.</p><hr><h3 id="bug-fix-—-remounted-item-appeared-as-a-duplicate-hover-target" tabindex="-1">Bug fix — remounted item appeared as a duplicate hover target <a class="header-anchor" href="#bug-fix-—-remounted-item-appeared-as-a-duplicate-hover-target" aria-label="Permalink to &quot;Bug fix — remounted item appeared as a duplicate hover target&quot;">​</a></h3><p><strong>Symptom.</strong> After scroll-away/scroll-back the freshly mounted item could be hovered and sorted as if it were a separate item — while the original dragged item was still floating in the overlay.</p><p><strong>Root cause.</strong> On remount, <code>onMounted</code> in <code>makeDraggable</code> registered the new element in <code>draggableMap</code> as a brand-new entry while the old (stale) element was still registered in <code>draggingMap</code>. The engine saw two entities with the same data position.</p><p><strong>Fix.</strong> On <code>onMounted</code>, if an <code>id</code> is provided, the library checks whether an entry with that id already exists in <code>draggingMap</code>. If it does (i.e. this item is currently being dragged), the old key is deleted and the new element is inserted in its place. The drag continues normally with no duplication.</p><hr><h3 id="bug-fix-—-full-shadow-dom-support" tabindex="-1">Bug fix — full Shadow DOM support <a class="header-anchor" href="#bug-fix-—-full-shadow-dom-support" aria-label="Permalink to &quot;Bug fix — full Shadow DOM support&quot;">​</a></h3><p><strong>Two separate bugs were fixed. Together they enable full Shadow DOM support — including cross-boundary drag between a shadow list and a regular list.</strong></p><h4 id="part-1-—-drag-never-started-inside-a-shadow-root" tabindex="-1">Part 1 — drag never started inside a shadow root <a class="header-anchor" href="#part-1-—-drag-never-started-inside-a-shadow-root" aria-label="Permalink to &quot;Part 1 — drag never started inside a shadow root&quot;">​</a></h4><p><strong>Symptom.</strong> Draggable elements mounted inside a shadow root (<code>attachShadow</code>) could not be dragged at all — pointer events were silently ignored and drag never started.</p><p><strong>Root cause.</strong> The library&#39;s pointer event listener reads <code>event.target</code> to find the draggable element. When a pointer event crosses a shadow root boundary, the browser <strong>re-targets</strong> it — <code>event.target</code> becomes the shadow host element (the outer <code>&lt;div&gt;</code>), not the actual element the user touched. The host is not registered as a draggable, so the engine did nothing.</p><p><strong>Fix.</strong> All pointer handlers now resolve the real target via <code>event.composedPath()[0]</code> instead of <code>event.target</code>. <code>composedPath</code> returns the full event path including elements inside shadow roots, so the first entry is always the actual element that received the event — even across shadow boundaries.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Before</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.target </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HTMLElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// After — works across shadow root boundaries</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> target</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (event.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">composedPath</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">?.()[</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">??</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> event.target) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> HTMLElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><h4 id="part-2-—-one-provider-for-both-regular-dom-and-shadow-dom" tabindex="-1">Part 2 — one provider for both regular DOM and shadow DOM <a class="header-anchor" href="#part-2-—-one-provider-for-both-regular-dom-and-shadow-dom" aria-label="Permalink to &quot;Part 2 — one provider for both regular DOM and shadow DOM&quot;">​</a></h4><p><strong>The old approach</strong> wrapped the shadow app in its own <code>DnDProvider</code>. This created two completely isolated DnD contexts — drag sessions in the shadow root were invisible to the outer provider and vice versa. Sorting between the two lists was impossible.</p><p><strong>The fix</strong> is to share the outer provider with the inner Vue app via <code>app.provide</code> using the newly exported <code>injectionKey</code>. Since pointer events from inside the shadow root already reach <code>document</code> (where the outer provider listens), and <code>composedPath()[0]</code> resolves the real element, all that was missing was the elements being registered in the same maps.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { inject } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;vue&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { injectionKey } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;@vue-dnd-kit/core&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// Inside a component that is a descendant of the outer DnDProvider:</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> outerProvider</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> inject</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(injectionKey);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">onMounted</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(() </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> shadow</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> host.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">attachShadow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ mode: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;open&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> container</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> document.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">createElement</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;div&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  shadow.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">appendChild</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(container);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // No DnDProvider wrapper — share the outer provider directly</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  createApp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({ </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">render</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> h</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(YourShadowList) })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">provide</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(injectionKey, outerProvider)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">mount</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(container);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">});</span></span></code></pre></div><p>Result: shadow DOM elements and regular DOM elements participate in the same drag session. You can sort within each list and drag items across the boundary between them.</p><hr><h2 id="v2-2-0" tabindex="-1">v2.2.0 <a class="header-anchor" href="#v2-2-0" aria-label="Permalink to &quot;v2.2.0&quot;">​</a></h2><ul><li>Added grid layout example</li><li>Various stability improvements</li></ul><h2 id="v2-1-0" tabindex="-1">v2.1.0 <a class="header-anchor" href="#v2-1-0" aria-label="Permalink to &quot;v2.1.0&quot;">​</a></h2><ul><li><code>makeSelectionArea</code> — box-select with multi-drag support</li><li><code>makeAutoScroll</code> — automatic container scroll during drag</li><li>Keyboard navigation (Enter / Space / Arrows / Escape)</li></ul><h2 id="v2-0-0" tabindex="-1">v2.0.0 <a class="header-anchor" href="#v2-0-0" aria-label="Permalink to &quot;v2.0.0&quot;">​</a></h2><ul><li>Full rewrite. Composable-first API: <code>makeDraggable</code>, <code>makeDroppable</code>, <code>makeConstraintArea</code>, <code>makeSelectionArea</code></li><li><code>DnDProvider</code> as the scope boundary (multiple independent providers on one page)</li><li><code>suggestSort</code>, <code>suggestSwap</code>, <code>suggestCopy</code>, <code>suggestRemove</code> helpers on <code>event.helpers</code></li><li>Touch support</li><li>Zero external dependencies</li></ul>`,49)])])}const k=s(n,[["render",o]]);export{c as __pageData,k as default};
